{
    "tasks": [
        {
            "type": "shell",
            "label": "build",
            "command": "pebble build",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Pebble Build"
        },
        {
            "type": "shell",
            "label": "Build & Run",
            "command": "pebble build && pebble install --emulator emery",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Pebble Build & Run"
        }
    ],
    "version": "2.0.0"
}